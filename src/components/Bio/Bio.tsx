import { AppBar, Box, Toolbar} from '@mui/material'
import React from 'react';

function Bio() {
  return ( 
    <Box>
      <AppBar>
        <Toolbar>

        </Toolbar>
      </AppBar> 
    </Box>
    );
}

export default Bio;