import { AppBar, Box, Toolbar} from '@mui/material'

function NavBar() {
  return ( 
    <Box>
      <AppBar>
        <Toolbar>

        </Toolbar>
      </AppBar> 
    </Box>
    );
}

export default NavBar;
