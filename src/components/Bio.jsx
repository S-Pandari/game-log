import { AppBar, Box, Toolbar} from '@mui/material'

function Bio() {
  return ( 
    <Box>
      <AppBar>
        <Toolbar>

        </Toolbar>
      </AppBar> 
    </Box>
    );
}

export default Bio;